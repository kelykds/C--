#include <bits/stdc++.h>

using namespace std;

struct {
    int id;
    int pontuação;
}

int main() {

    int g, p;
    cin >> g >> p;
    // se botar em vector, presta? não complica dps?... struct..
    vector<vector<int>> chegada;

    for (int i = 0; i < g; i++) {
        for (int j = 0; j < p; i++) {
             
        }
    }

    return 0;
}
